# Deployment Notes

- Recommended domain: `wilcomduncan.com`
- Avoid using a subdomain for the final client presentation.
- Set `NEXT_PUBLIC_SITE_URL` to `https://wilcomduncan.com` after the domain is connected.
- Keep `metadataBase` in Next.js metadata pointed at the custom domain.
- Update sitemap and robots configuration if either is introduced.
- Update social sharing URLs and images when production social assets are finalized.
